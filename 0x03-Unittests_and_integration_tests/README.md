# Project Tests for utils.access_nested_map

This project contains an utility function `access_nested_map` 
and unit tests
that verify its behavior for multiple nested mapping inputs and paths.

## Running tests

python -m unittest tests/test_utils.py -v

## To make it executable

chmod +x test_utils.py

## To check with pycodestyle
pycodestyle filename
